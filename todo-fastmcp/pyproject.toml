[project]
name = "todo-fastmcp"
version = "0.1.0"
description = "FastMCP-based MCP server providing in-memory todo CRUD tools"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=3.0.0",
    "python-dotenv>=1.0.0"
]
